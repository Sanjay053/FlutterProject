import 'package:flutter/material.dart';

final ButtonStyle greenButton = ElevatedButton.styleFrom(
    minimumSize: const Size(Checkbox.width, 50),
    backgroundColor: Colors.green,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.all(
        Radius.circular(50),
      ),
    ));

final ButtonStyle blueButton = ElevatedButton.styleFrom(
    minimumSize: const Size(Checkbox.width, 50),
    backgroundColor: Colors.blue,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.all(
        Radius.circular(50),
      ),
    ));

final ButtonStyle orageButton = ElevatedButton.styleFrom(
    minimumSize: const Size(Checkbox.width, 50),
    backgroundColor: Colors.orange,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.all(
        Radius.circular(50),
      ),
    ));

final ButtonStyle redButton = ElevatedButton.styleFrom(
    minimumSize: const Size(Checkbox.width, 50),
    backgroundColor: Colors.red,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.all(
        Radius.circular(50),
      ),
    ));